{
  "build": {
    "builder": "https://github.com/heroku/heroku-buildpack-php.git",
    "installCommand": "composer install --no-dev --optimize-autoloader",
    "postBuildCommand": "php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache"
  },
  "deploy": {
    "startCommand": "vendor/bin/heroku-php-apache2 public/"
  }
}